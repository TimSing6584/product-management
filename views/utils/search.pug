mixin search(last_search_word)
    form(id="form-search")
        .input-group
            input(type="text" placeholder="Enter keyword" name="search_key_word" class="form-control" value=last_search_word)
            button(class="btn btn-success" type="submit") Search
            button(class="btn btn-secondary" type="submit") Clear