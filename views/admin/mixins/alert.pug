mixin alert(time)
    - if (messages.success)
        .message.info
            div(
                class="alert alert-success"
                show-alert
                data-time=time
            ) #{messages.success} <button close-alert type="button" class="btn btn-outline-secondary">X</button>